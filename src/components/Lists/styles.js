import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  container: {
      padding: "10px"
  },
  formControl: {
      marginLeft: "25px",
      width: "150px"
  }
});

export default useStyles;